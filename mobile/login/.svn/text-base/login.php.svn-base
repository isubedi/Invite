<?php
	require_once("../../includes/db.php");
	require_once("../../core/db_func/secure.php");
	require_once("../../core/auth/auth_func.php");
	require_once("../../core/errors/get_error_from_code.php");
	$email = clean($_GET['username']); 
	$password = md5(clean($_GET['password']));

	$result = doLogin($email, $password);
	
	if (substr($result, 0, 4) == "Err_") {
		echo "Err_".getErrorMessage(substr($result, 4));
	}else{
		echo $result;
	}

?>